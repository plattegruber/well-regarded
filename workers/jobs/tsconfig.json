{
  "extends": "@wellregarded/tsconfig/worker.json",
  "compilerOptions": {
    // Workers runtime types for src/; node types for vitest.config.ts and
    // the Node-run test/ suites (same pattern as workers/pipeline).
    "types": ["@cloudflare/workers-types", "node"]
  },
  "include": ["src", "test", "vitest.config.ts"]
}
