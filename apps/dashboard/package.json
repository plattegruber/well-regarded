{
  "name": "@wellregarded/dashboard",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "lint": "biome check .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@wellregarded/tsconfig": "workspace:*",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
